exports.handleError = (error, email, notes) => {

    //log error 
    //log error stack
    //log which function
    console.log()

    //email error if its important enough

    //return something to passback up to parent function

        

}